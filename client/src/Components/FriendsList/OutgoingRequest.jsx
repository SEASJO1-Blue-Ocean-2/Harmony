import React from 'react';

const OutgoingRequest = (props) => {
  const temp = '';
  return (
    <div>
      this is what one outgoing request looks like
    </div>
  );
};

export default OutgoingRequest;
