import React from 'react';

const OutgoingButton = (props) => {
  const temp = '';
  return (
    <div>
      <button>Outgoing</button>
      {/* //this button will route to the outgoing friend requests still pending */}
    </div>
  )
}

export default OutgoingButton;
